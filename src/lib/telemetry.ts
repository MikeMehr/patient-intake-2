// Telemetry initializer stubbed for now to avoid bundling optional deps in dev/edge.
// If you need Application Insights, reintroduce a guarded import and configure in production only.
export async function initTelemetry(): Promise<void> {
  return;
}

